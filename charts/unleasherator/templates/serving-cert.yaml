apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "unleasherator.fullname" . }}-serving-cert
  labels:
  {{- include "unleasherator.labels" . | nindent 4 }}
spec:
  dnsNames:
  - '{{ include "unleasherator.fullname" . }}-webhook-service.{{ .Release.Namespace
    }}.svc'
  - '{{ include "unleasherator.fullname" . }}-webhook-service.{{ .Release.Namespace
    }}.svc.{{ .Values.kubernetesClusterDomain }}'
  issuerRef:
    kind: Issuer
    name: '{{ include "unleasherator.fullname" . }}-selfsigned-issuer'
  secretName: webhook-server-cert