apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: releasechannels.unleash.nais.io
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  labels:
  {{- include "unleasherator-crds.labels" . | nindent 4 }}
spec:
  group: unleash.nais.io
  names:
    kind: ReleaseChannel
    listKind: ReleaseChannelList
    plural: releasechannels
    singular: releasechannel
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.version
      name: Version
      type: string
    - jsonPath: .status.instances
      name: Instances
      type: integer
    - jsonPath: .status.instancesUpToDate
      name: InstancesUpToDate
      type: integer
    - jsonPath: .status.canaryInstances
      name: Canaries
      type: integer
    - jsonPath: .status.canaryInstancesUpToDate
      name: CanariesUpToDate
      type: integer
    name: v1
    schema:
      openAPIV3Schema:
        description: ReleaseChannel is the Schema for the releasechannels API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ReleaseChannelSpec defines the desired state of ReleaseChannel
            properties:
              healthChecks:
                description: HealthChecks defines health validation configuration
                properties:
                  enabled:
                    default: true
                    description: Enabled determines if health checks are performed after
                      deployment
                    type: boolean
                  endpoint:
                    description: Endpoint is a custom health check endpoint (optional)
                    type: string
                  initialDelay:
                    default: 30s
                    description: InitialDelay is the wait time after deployment before
                      starting health checks
                    type: string
                  timeout:
                    default: 5m
                    description: Timeout is the maximum time to wait for health check
                      to pass
                    type: string
                type: object
              image:
                description: Image is the Docker image to deploy for the release channel.
                type: string
              rollback:
                description: Rollback defines rollback configuration
                properties:
                  enabled:
                    default: false
                    description: Enabled determines if automatic rollback is enabled
                      on failure
                    type: boolean
                  onFailure:
                    default: true
                    description: OnFailure determines if rollback should trigger on
                      deployment failure
                    type: boolean
                  previousImage:
                    description: PreviousImage is the image to rollback to (auto-populated)
                    type: string
                type: object
              strategy:
                description: Strategy defines the deployment strategy.
                properties:
                  batchInterval:
                    default: 30s
                    description: BatchInterval defines wait time between deployment
                      batches
                    type: string
                  canary:
                    description: Canary defines the canary strategy.
                    properties:
                      enabled:
                        default: false
                        description: Enabled defines if canary is enabled.
                        type: boolean
                      podSelector:
                        description: |-
                          LabelSelector is the label selector for the canary instances.
                          Unleash instances matching this selector will be considered canary instances.
                        properties:
                          matchExpressions:
                            description: matchExpressions is a list of label selector
                              requirements. The requirements are ANDed.
                            items:
                              description: |-
                                A label selector requirement is a selector that contains values, a key, and an operator that
                                relates the key and values.
                              properties:
                                key:
                                  description: key is the label key that the selector
                                    applies to.
                                  type: string
                                operator:
                                  description: |-
                                    operator represents a key's relationship to a set of values.
                                    Valid operators are In, NotIn, Exists and DoesNotExist.
                                  type: string
                                values:
                                  description: |-
                                    values is an array of string values. If the operator is In or NotIn,
                                    the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                    the values array must be empty. This array is replaced during a strategic
                                    merge patch.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                              required:
                              - key
                              - operator
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: |-
                              matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                              map is equivalent to an element of matchExpressions, whose key field is "key", the
                              operator is "In", and the values array contains only "value". The requirements are ANDed.
                            type: object
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  maxParallel:
                    default: 1
                    description: MaxParallel defines the maximum number of instances
                      to deploy in parallel.
                    maximum: 50
                    minimum: 1
                    type: integer
                  maxUpgradeTime:
                    default: 10m
                    description: MaxUpgradeTime defines maximum time to wait for all
                      upgrades to complete
                    type: string
                type: object
            required:
            - image
            type: object
          status:
            description: ReleaseChannelStatus defines the observed state of ReleaseChannel
            properties:
              canaryInstances:
                default: 0
                description: CanaryInstances is the number of canary instances for the
                  release channel.
                type: integer
              canaryInstancesUpToDate:
                default: 0
                description: CanaryInstancesUpToDate is the number of canary instances
                  that are up to date.
                type: integer
              completed:
                default: false
                description: Rollout is true if the release channel has completed rollout
                  successfully.
                type: boolean
              conditions:
                description: Conditions is a list of conditions for the release channel.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              estimatedCompletion:
                description: EstimatedCompletion is the estimated completion time
                format: date-time
                type: string
              failureReason:
                description: FailureReason contains the reason for rollout failure
                type: string
              instanceStatus:
                description: InstanceStatus provides detailed status for each instance
                items:
                  description: InstanceStatus provides detailed status for individual
                    instances
                  properties:
                    endTime:
                      description: EndTime when upgrade completed for this instance
                      format: date-time
                      type: string
                    message:
                      description: Message with additional details
                      type: string
                    name:
                      description: Name of the Unleash instance
                      type: string
                    phase:
                      description: Phase of this instance's upgrade
                      type: string
                    ready:
                      description: Ready indicates if instance is ready after upgrade
                      type: boolean
                    startTime:
                      description: StartTime when upgrade started for this instance
                      format: date-time
                      type: string
                  type: object
                type: array
              instances:
                default: 0
                description: Instances is the number of instances for the release channel.
                type: integer
              instancesUpToDate:
                default: 0
                description: InstancesUpToDate is the number of instances that are up
                  to date.
                type: integer
              lastReconcileTime:
                description: LastReconcileTime is the last time the release channel
                  was reconciled.
                format: date-time
                type: string
              phase:
                description: Phase represents the current phase of the rollout
                type: string
              progress:
                description: Progress represents rollout progress as a percentage (0-100)
                maximum: 100
                minimum: 0
                type: integer
              startTime:
                description: StartTime is when the current rollout started
                format: date-time
                type: string
              version:
                default: unknown
                description: Version is the version of the release channel.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
